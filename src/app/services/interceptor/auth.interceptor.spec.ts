// import {TestBed} from '@angular/core/testing';
// import {HttpClientTestingModule, HttpTestingController} from '@angular/common/http/testing';
// import {MockProvider} from 'ng-mocks';
// import {IdpInterceptor} from './idp.interceptor';
// import {IdpService} from './idp.service';
// import {LogService} from '@cnj/uikit';

// describe('IdpInterceptor', () => {
//   let interceptor: IdpInterceptor;
//   let httpMock: HttpTestingController;

//   beforeEach(() => {
//     TestBed.configureTestingModule({
//       imports: [HttpClientTestingModule],
//       providers: [IdpInterceptor, MockProvider(IdpService), MockProvider(LogService)]
//     });
//     interceptor = TestBed.inject(IdpInterceptor);
//     httpMock = TestBed.inject(HttpTestingController);
//   });

//   afterEach(() => {
//     TestBed.resetTestingModule();
//   });

//   afterEach(() => httpMock.verify());

//   it('should be created', () => expect(interceptor).toBeTruthy());
// });
